import moment from '../../libs/Date'

/**
 *
 */
export default class DateGlobalInterface {
  /**
   * @return {*}
   */
  now() {
    return {}
  }
  
  /**
   * @param {*} datetime
   * @param {string} type
   * @return {string}
   */
  format(datetime, type = 'datetime') {
    return ''
  }
}
